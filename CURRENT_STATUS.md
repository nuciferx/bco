# CURRENT_STATUS — BCO Telegram Bot

อัปเดตล่าสุด: **24 ส.ค. 2569**

## สถานะตอนนี้: 🟢 ใช้งานได้

| | |
|---|---|
| บอทหลัก | Cloudflare Worker — `https://bco-telegram-bot.ideaplanstudio.workers.dev` |
| Telegram | `@bcobma_bot` (ชื่อ "Bco bot") · chat ที่ตั้งไว้ id `957180305` |
| สถานะ auth | **ล็อกอินอยู่** — token หมดอายุ **27 ส.ค. 2569 05:45** |
| ทดสอบล่าสุด | `/status` และ `/otp` ตอบกลับปกติ ผู้ใช้ยืนยันว่าเข้าใช้งานได้ |
| บอท Python | ใช้ได้ (สำรอง) — ล็อกอินสำเร็จ ดึง `/top` ได้ข้อมูลจริง |

## ⏰ ต้องทำทุก ~3 วัน

token หมดอายุทุก 3 วัน **ต่ออายุอัตโนมัติไม่ได้** (พิสูจน์แล้ว — ดู README)

พอหมดอายุ ตัวเฝ้าดูจะเตือนเข้า Telegram เอง (เช็คทุก 30 นาที) → ตอบในแชตส่วนตัวว่า

```
/otp <รหัส 6 หลักจากแอป OTP ของ กทม.>
```

Worker จะ login เองแล้วเก็บ token ลง KV ให้ ไม่ต้องแตะเครื่อง

## ที่ทำเสร็จในรอบนี้ (24 ส.ค. 2569)

- ล็อกอิน BCO สำเร็จทั้งฝั่งเครื่องและฝั่ง Worker
- ติดตั้ง `pyotp` + `staticmap` ที่ขาดอยู่
- แก้บั๊ก `token_manager.py` ไม่โหลด `.env` ตอนรันเป็น CLI
- พิสูจน์และจดบันทึกเรื่อง auth ครบ 4 ข้อ (ดู README + AGENTS.md)
- commit งานค้างจากเดือน เม.ย. (คำสั่ง `/polygon` + endpoint `write_map`)

## ที่ยังไม่ได้ทำ

- **โค้ดในเครื่องใหม่กว่าที่ deploy อยู่** — `/polygon` deploy ไปแล้ว 22 เม.ย.
  แต่หลังจากนั้นยังไม่ได้ deploy ซ้ำ ถ้าแก้ `index.ts` อีกต้อง `cd telegram-worker && npx wrangler deploy`
- **บอทยังกดส่งงานไม่ได้** — อ่านอย่างเดียว ยังไม่มีคำสั่งเขียนกลับเข้าระบบ (ดู NEXT_ACTION.md)
